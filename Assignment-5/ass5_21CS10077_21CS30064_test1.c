// Checking for Declarations (variables(int, float, char), arrays) and Arithmetic Expressions
        

int main () {
    // Variable Declaration
    int x = 120;
    int y = 17;
    char ch = 'c', d = 'a';     // character definitions
    
    float f = 3.2;
    int w[10];                      // Array declaration
    int a = 4, *p, g;               // pointer declaration
    
    int i, j, k, l, m, n, o,e;
    
     // Arithmetic Operations
    i = x + y;
    j = x - y;
    k = x * y;
    l = x / y;
    m = x % y;
    n = x & y;
    o = x | y;
    
   // Shift Expressions
    y = i << 2;
    x = i >> 1;


    // Checking for Assignment Expression
    e=y;

    return 0;
}